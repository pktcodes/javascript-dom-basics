// Logical Operators
// (|| - OR), (&& - AND), !

const name = 'peter';
const age = 24;

if (name !== 'peter' || age === 24) {
  console.log('hello there, ' + name);
} else {
  console.log('wrong values');
}
